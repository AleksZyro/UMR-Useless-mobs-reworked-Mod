# Common Code

Put code or resources here when more than one mob module needs it.

Examples:

- shared helpers
- shared base classes
- shared data or resource templates
- cross-mob utilities

The current main mod entrypoint and Forge metadata still stay in `src/main/java` and `src/main/resources`.
